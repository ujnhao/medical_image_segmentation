{
  "train_fid": "list/mr_train_list",
  "val_fid": "list/mr_val_list",
  "output_path": "./tmp_exps/mr_baseline",
  "num_cls": 5,

  "#restore": "# Set True if resume training from stored model",
  "restore": false,

  "#lr_update_flag": "# Set True if want to use a new learning rate for fine-tuning",
  "lr_update_flag": false,
  "epochs": 5000,
  "optimizer": "adam"
}
